package Øvelse9;

public class User {
    private String fullName;
    private String userID;

    public boolean validUserID(String fullName, String userID) {
        this.fullName = fullName;
        this.userID = userID;

        char[] chars = userID.toCharArray();
        for (char c : chars) {
            if (!userID.)
        }

    }
}
